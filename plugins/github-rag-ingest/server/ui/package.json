{
  "name": "github-rag-ingest-ui",
  "version": "1.0.0",
  "description": "UI components for GitHub RAG Ingestion Plugin",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rm -rf dist/*"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  }
}
